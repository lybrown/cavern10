%::abbrev = qw(
    MOVE     M.
    FOR      F.
    GOTO     G.
    WHILE    W.
    WEND     WE.
    ELSE     EL.
    ENDIF    END.
    DPOKE    DP.
    RETURN   RET.
    GRAPHICS GR.
    SOUND    SO.
    GOSUB    GOS.
    PAUSE    PA.
);

1;
